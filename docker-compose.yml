version: "3.8"

services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: optuna_user
      POSTGRES_PASSWORD: optuna_pass
      POSTGRES_DB: optuna_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
